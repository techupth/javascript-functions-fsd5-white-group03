function findMinNumber (a,b){
    /*if(a > b)
        return (b);
    if(b > a)
        return (a);*/
    return (Math.min(a,b));
}
const minNumber = findMinNumber(10, 20);

console.log(minNumber); // 10
